ジャンル一覧・追加

<table class="table table-hover table-inverse" style="width:35%;margin-left:5%">
  <tbody>
    <%= form_with model: Genre.new, url: admin_genres_path,method: :post do |f| %>
    <tr>
      <td>ジャンル名</td>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit '新規登録', class: "btn btn-success" %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<table class="table table-hover table-inverse" style="width:30%;margin-left:5%;margin-top:40px">
  <thead class="bg-secondary">
    <tr>
      <th>ジャンル</th>
      <th></th>
     </tr>
  </thead>
  <tbody>
  <% @genres.each do |genre| %>
  <tr>
   <td><%= genre.name %></td>
   <td><%= link_to "編集する", edit_admin_genre_path(genre), class: "btn btn-success" %></td>
  <tr>
  <% end %>
 </tbody>
</table>